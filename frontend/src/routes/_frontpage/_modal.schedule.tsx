import { createFileRoute } from "@tanstack/react-router";

export const Route = createFileRoute("/_frontpage/_modal/schedule")({
    component: () => <div>Hello /_frontpage/_modal/schedule!</div>,
});
